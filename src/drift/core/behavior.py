"""Behavior dataclass — bundles a judge, scenario directory, and reward config
under a unique name. Registered in the behavior registry.

Adding a new behavior (V1: E, B; V2: Volnix goal-hijacking) means defining a
new Behavior here — nothing in env/training/eval should need to change.

Filled in by Unit 0.2.
"""
