"""Common Scenario YAML schema.

A `behavior_config` sub-field carries behavior-specific data (e.g., hidden
secret for E, assigned persona for B) so the loader stays behavior-agnostic.

Filled in by Unit 2.1.
"""
