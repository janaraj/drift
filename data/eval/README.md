# data/eval/

Baseline and checkpoint evaluation results (versioned).

Each file is a JSON record of an eval sweep: (attacker × target × scenarios)
→ ASR, turns-to-success distribution, and metadata. These are research
artifacts and are tracked in git.

Populated starting in Unit 3.5 (baseline matrices) through Unit 6.2 (final
transfer matrix).
