# data/scenarios/e/

Hand-authored scenarios for **E — system prompt extraction**.

Each scenario is a YAML file conforming to the common schema in
`src/drift/scenarios/schema.py`, with `behavior_config.secret` carrying
the hidden string the attacker must elicit.

Populated by Unit 2.4 (initial 5) and Unit 2.6 (complete 30-50).
